# URL of your uwu-algo-oraclesigner instance (Render, Fly, local, etc.)
NEXT_PUBLIC_ORACLE_API_URL=http://localhost:3001

# "mock" (default) = local simulation, no real Setu calls
# "setu" = real Setu sandbox redirect + bank verification
NEXT_PUBLIC_VERIFICATION_MODE=mock
